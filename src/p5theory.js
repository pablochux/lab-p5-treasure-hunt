// P5 is a library that

// For P5, we use some functions

function setup() {} // Only calls once, as soon as the page loads
// To create a canvas we use the createCanvas() with two parameters, width and height
// These constants are usually created in the constants.js (keep them all organized)

function draw() {}
// To create a rectangle -> rect(X, Y, width, heigth) in the draw function

function keyPressed() {}

// POSITION IN P5
// The top right corner is 0, 0
// If we add numbers to startingY we will go down
// If we add numbers to startingX we will to the right

// how it works
// 0,0             0,100

// 100,0           100,100
